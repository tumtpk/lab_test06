version: 2.1
orbs:
  python: circleci/python@1.4.0

workflows:
  sample:
    jobs:
      - build-and-test
jobs:
  build-and-test:
    docker:
      - image: messa/circleci-python-with-mongodb:latest

    steps:
      - checkout
      # - setup_remote_docker
      - run:
          name: create user
          command: mongo --eval 'db.createUser({ "user":"root", "pwd":"root", "roles":[ { "role":"userAdminAnyDatabase", "db":"admin" } ] })'
      - python/install-packages:
          pkg-manager: pip
      - run: pip install pytest requests
      - run:
          name: Run tests
          command: python -m pytest
